import React from 'react';
import Login from './login'; // ✅ Make sure this file exists in src/
import './App.css'; // optional — keep this if you have global styles

function App() {
  return (
    <div>
      <Login />
    </div>
  );
}

export default App;
